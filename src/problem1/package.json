{
    "name": "sum-to-n",
    "version": "1.0.0",
    "description": "Three implementations of sum to n function",
    "type": "module",
    "scripts": {
        "test": "vitest",
        "test:run": "vitest run"
    },
    "devDependencies": {
        "vitest": "^1.2.0"
    }
}